$("#activity-<%=@resource.id%>").html("<%= escape_javascript(render partial: 'activity_row', locals: { activity: @resource })%>");
startActivity();
restartActivity();
finishActivity();
showActivityInformation();

<% if @resource.done? %>
  toastr.success("<%= t('views.dashboard.finished_success') %>")
<% else %>
  toastr.error("<%= t('views.dashboard.checkbox_pendencies') %>")
<% end %>
