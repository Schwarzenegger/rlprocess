$('div#activity-container').html("<%= escape_javascript(render partial: 'activity') %>");
$(".modal-title").html("<%=@resource.name%>");
showModalWindow("activity-modal");
General.enableICheck();
whenOptionIsChecked();
