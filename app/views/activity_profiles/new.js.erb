$('div#activity-profile-form-container').html("<%= escape_javascript(render partial: 'form') %>");
$(".modal-title").html("<%=t('actions.add_one', model: t('activerecord.models.activity_profile').downcase)%>");
showModalWindow("activity-profile-modal");
setupComponents()
