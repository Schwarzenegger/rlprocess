$('div#activity-information-container').html("<%= escape_javascript(render partial: 'activity') %>");
$(".modal-title").html("<%=@resource.name%>");
showModalWindow("activity-information-modal");
General.enableICheck();
whenOptionIsChecked();
