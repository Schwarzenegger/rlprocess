<%
  form_url = url_for(controller: "activity_profiles", action: "create")
  if !@resource.new_record?
    form_url = url_for(controller: "activity_profiles", action: "update", id: @resource.id)
  end
%>

<%= simple_form_for(@resource, url: form_url, authenticity_token: true,
    method: @resource.new_record? ? "POST" : "PUT", remote: true,
    wrapper: :vertical_form, html: { class: "form-vertical", role: "form", }) do |f| %>

    <div class="row">
      <div class="col-xs-12">
        <%= f.input :name %>
      </div>
    </div>

<% end %>
