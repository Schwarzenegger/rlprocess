$("#activity-<%=@resource.id%>").html("<%= escape_javascript(render partial: 'activity_row', locals: { activity: @resource })%>");
startActivity();
restartActivity();
finishActivity();
showActivityInformation();

<% if @resource.todo? %>
  toastr.success("<%= t('views.dashboard.restarted_success') %>")
<% else %>
  toastr.error("<%= t('views.dashboard.generic_status_error') %>")
<% end %>
