<% if @resource_valid %>
  window.location = "<%=@location%>";
<% else %>
  $('div#user-form-container').html("<%= escape_javascript(render partial: 'form') %>");
  setupComponents()
  setupFrequencyFields()
  onFrequenceChange();
  handleCheckBoxOptions();
<% end %>
